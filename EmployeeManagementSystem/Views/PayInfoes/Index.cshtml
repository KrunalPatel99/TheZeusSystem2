@model IEnumerable<EmployeeManagementSystem.Models.PayInfo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New Pay Stub", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmplyeeInfo.EmployeeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Month)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmplyeeInfo.EmployeeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Month)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PayStubId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PayStubId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PayStubId })
        </td>
    </tr>
}

</table>
